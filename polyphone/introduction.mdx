---
title: "Polyphone"
sidebarTitle: "Overview"
description: "Bring PolyAI voice agents directly to your website — a configurable voice widget that gives customers a faster, lower-friction way to get help."
keywords:
  - polyphone
  - voice widget
  - browser voice
  - click to call
  - voice channel
  - webrtc
---

Polyphone puts your PolyAI voice agent **directly on your website**. Visitors click "Talk to our agent" and start speaking from the page they're already on — no number to dial, no app to install, no callback to wait for. You ship it from **Deployment > Widget** in Agent Studio in minutes.

{/* TODO image — alt: "Polyphone launcher in the bottom-right of a website" — path: /images/polyphone/hero-launcher.png */}
> A fast way to bring PolyAI voice agents onto your website — a simple voice entry point for your customers, and a faster path to deploy, test, and scale digital voice experiences for your team.

## Why Polyphone

Polyphone replaces a stack of awkward alternatives:

- **vs. a phone number on the page.** No PSTN minutes, no IVR menu, no waiting for the visitor to find their phone. Intent gets captured at the moment of curiosity — on the page where they're already engaged.
- **vs. a "request a callback" form.** No lag between intent and conversation. The agent picks up immediately, while the question is still in the visitor's head.
- **vs. a chatbot.** Voice is faster and more natural for complex questions, accessibility, and hands-free use on mobile. Customers who'd never type a paragraph will happily say one.
- **vs. internal Studio testing tools.** Calls land on the same production stack — Iris, the WebRTC gateway, the live agent variant — that real customers will hit. You're testing what you'll ship, in front of stakeholders, on the actual website.

**The business case in one line:** Polyphone gives you the conversion mechanics of a phone agent without the friction of a phone number — and the production fidelity of a real call without the cost of provisioning one for every demo or pilot.

## Customer value

- **Faster deployment.** Ship a website voice entry point in minutes — paste a script tag, publish a variant, you're live.
- **More choice for customers.** Visitors who'd rather speak than type get a one-click route to your agent. Multilingual agents are surfaced in whichever languages they're configured for.
- **Easier testing and iteration.** Validate widget and agent behaviour from a hosted preview before any code goes near your site.
- **Brand and site flexibility.** Multi-brand or multi-site? Spin up separate widget variants per site, with light or dark visual treatments to match each brand.
- **A faster path for existing PolyAI customers.** Enterprise customers will be able to connect their existing voice agents to Polyphone for rapid website deployment once it's released to Enterprise.

## When end users reach for it

Polyphone is especially useful when:

- The customer can explain the issue faster by speaking than typing.
- The issue is urgent.
- The customer is on mobile, multitasking, or driving.
- Voice is more accessible than text for the user.
- The customer wants an immediate back-and-forth, not a queued reply.

The result is a lower-effort route to support.

## Best-fit launch use cases

Polyphone is strongest when deployed in **targeted, high-value areas of your website** where customers are already likely to need help:

- Support landing pages.
- Contact-us pages.
- Logged-in customer-service portals (claims, bookings, accounts, orders).
- Multilingual support journeys where customers may prefer speaking in their own language.
- High-intent conversion pages.
- Controlled pilots with known traffic volumes.
- Multi-brand sites that need separate voice experiences per brand.

> Start with a targeted page, validate engagement and call quality, then expand based on performance and demand.

## What it does

Polyphone reuses the same agent that answers your phone numbers — same voice, same conversation flow. It's just another way for customers to reach it: through the browser instead of the phone network.

For text-only conversations on your site, use [Webchat](/webchat/introduction). For phone calls from anywhere, use [Numbers](/telephony/introduction). Polyphone, Webchat, and Numbers all share the same agent — you can run any combination.

{/* TODO image — alt: "Polyphone, Numbers, and Webchat all routing to the same PolyAI agent" — path: /images/polyphone/channel-mix.png */}
## Polyphone in the channel mix

Polyphone is a **voice channel**, just like a phone number. The only thing that changes between them is how the audio gets to PolyAI.

| | Polyphone | Numbers (telephony) | Chat (webchat) | Voice (TTS settings) |
| --- | --- | --- | --- | --- |
| What customers do | Click a button on your site | Dial a phone number | Type in a widget on your site | – (configures how your agent sounds) |
| Modality | Voice | Voice | Text | Shared by all voice channels |
| Where you set it up | Deployment > Widget | Channels > Numbers | Deployment > Widget | Channels > Voice |
| Docs | This section | [Numbers](/telephony/introduction) | [Webchat](/webchat/introduction) | [Voice](/voice/introduction) |

Polyphone and Numbers are two ways into the same voice agent. They share the agent's spoken [voice](/voice/introduction) (set once, under **Channels > Voice**) and the same conversation logic — switching a visitor between them doesn't require a separate flow.

## Deployment options

Polyphone gives you two ways to ship: a **shareable page** for testing and demos, and an **embedded widget** for live traffic. Most teams start with the shareable page, validate the experience with stakeholders, then embed.

{/* TODO image — alt: "Shareable page on the left, embedded website widget on the right" — path: /images/polyphone/deployment-options.png */}
<CardGroup cols={2}>
  <Card title="Shareable page" icon="link" href="/widgets/test">
    A hosted page that lets teams and stakeholders try the voice agent without embedding it anywhere. Best for internal testing, customer demos, early stakeholder review, and fast validation before website deployment.
  </Card>
  <Card title="Website widget" icon="code" href="/widgets/install">
    A drop-in widget you embed on your website so end users can start a voice interaction directly from the site. Best for controlled pilots, high-intent support pages, logged-in portals, claims/booking/account journeys, and multi-brand websites.
  </Card>
</CardGroup>

## What you get out of the box

- **A configurable launcher** — colour, position, and label match your site, with light or dark visual wave treatments.
- **A consent step** — visitors agree to mic access and your privacy notice before the call connects.
- **Mute / end-call controls** — standard call UI built into the widget.
- **Basic call states** — connecting, in call, ended, and error states are surfaced to the user.
- **Cross-tab safety** — only one call can be active per visitor, even if they have multiple tabs open.
- **Domain allow-list** — the widget only loads on domains you've authorised.
- **Multiple widget variants** — separate widgets per brand, region, or site, configured independently.
- **Multilingual voice agents** — Polyphone surfaces whichever languages the connected PolyAI agent is configured for.

## Compliance and consent

Polyphone includes optional controls for privacy, disclaimer, and consent configuration. These controls are **off by default** but are easily activated — see [Widget configuration](/widgets/configure) for setup.

<Warning>
We recommend all customers review their compliance and disclosure requirements carefully. It is your responsibility to meet transparency, disclosure, and consent requirements in every jurisdiction you operate in. This is particularly relevant for EU clients and some US states.
</Warning>

## Where Polyphone fits today

Polyphone is purpose-built for **fast deployment, controlled rollout, and website-based voice access** — and that focus is what makes it shippable in minutes. A few things to keep in mind as you plan your rollout:

- **It's a voice entry point, not a handoff layer.** Polyphone runs the call end-to-end on the same PolyAI agent — voice-to-chat, chat-to-voice, and live-agent transfer aren't part of v1. If your launch journey needs a handoff today, pair Polyphone with [Numbers](/telephony/introduction) or [Webchat](/webchat/introduction) for that step.
- **It complements phone support — it doesn't replace it.** Use Polyphone alongside your existing phone channel to capture web visitors who'd rather not pick up the phone, while keeping your established hotlines for off-site customers.
- **It runs in the browser.** That means a fast install with no carrier provisioning, and it also means the call uses what the browser gives it — mic permissions, network conditions, and mobile-browser behaviour all matter. Test in your intended deployment environment before launch and you'll catch any environment-specific quirks early.

## Prerequisites

Before you create a Polyphone widget, you'll need:

- An Agent Studio project with a **published variant**.
- The **website domain(s)** where the widget will be embedded.
- Access to your website's HTML or your tag manager.
- An **HTTPS** site — browsers require a secure context for microphone access.

You don't need a chat agent to use Polyphone — you can ship a voice-only widget. (Behind the scenes, Polyphone and Webchat share a widget framework and a single Iris script endpoint, which is why both live under **Deployment > Widget** in Studio — you pick **Phone** or **Chat** as the widget type when you create one.)

## Channel pages

<CardGroup cols={2}>
  <Card title="Widget" href="/widgets/configure" icon="phone">
    Create, style, and configure a Polyphone widget in Agent Studio.
  </Card>
  <Card title="Installation" href="/widgets/install" icon="code">
    Embed the script tag on your website with HTML or Tag Manager.
  </Card>
  <Card title="Test widget" href="/widgets/test" icon="circle-play">
    Walk through a real call from the editor preview link.
  </Card>
  <Card title="Troubleshooting" href="/widgets/troubleshooting" icon="wrench">
    Mic permissions, ICE failures, cross-tab lock, and more.
  </Card>
</CardGroup>

## Technical details

<Accordion title="How a call connects">

```
Visitor's browser                           PolyAI
─────────────────────                       ──────────────
Polyphone iframe (mic, autoplay)
   │
   ├── HTTPS  ──────► Iris script endpoint  (loads widget JS)
   │
   └── WSS    ──────► WebRTC gateway        (signaling + Opus audio)
                            │
                            └─► your agent
```

Polyphone is loaded by a single `<script>` tag on your site. The script injects an iframe pinned to the bottom-right of the page. When a visitor clicks the launcher, the widget requests microphone access, opens a WebSocket to PolyAI's WebRTC gateway, and starts an Opus 48 kHz audio call with your agent.

Only **one Polyphone call can be active per browser** at a time. The widget enforces this with a cross-tab lock so a visitor with multiple tabs open cannot start parallel calls.

</Accordion>

<Accordion title="Polyphone vs. telephony, side by side">

| | Polyphone | Telephony |
| --- | --- | --- |
| Transport | WebRTC over the browser | Phone network (PSTN / SIP) |
| Visitor sees | A widget on your website | A phone number to dial |
| Latency / quality | Opus 48 kHz over WebRTC | Carrier-dependent |
| Setup | Embed a script tag | Provision a number, configure SIP |
| Best for | Web-first journeys, support deflection | Inbound call centres, omnichannel routing |

You can run both: Polyphone for visitors already on your site, telephony for callers from anywhere else. Both connect to the same agent.

</Accordion>

<Accordion title="Programmatic control (JavaScript SDK)">

The widget script also exposes a `window.PolyphoneAPI` object that lets your page drive the call programmatically — useful when you want to trigger Polyphone from a custom button, an in-app event, or an existing chat experience.

```js
// Open the widget and start a call in one step.
window.PolyphoneAPI.connectAndCall();

// Pass context to the agent mid-call.
window.PolyphoneAPI.sendInfo("user_signed_in", { userId: "abc123" });

// React to lifecycle events.
window.PolyphoneAPI.on("connected", () => console.log("Call connected"));
window.PolyphoneAPI.on("ended", () => console.log("Call ended"));
```

Available commands: `connect`, `disconnect`, `startCall`, `endCall`, `connectAndCall`, `mute`, `unmute`, `sendInfo`, `isConnected`, `getCurrentSession`. Events include `ready`, `connecting`, `connected`, `ringing`, `answered`, `ended`, `failed`, `muted`, `unmuted`, `transcription`, `infoSent`, `infoReceived`, `error`.

</Accordion>

<Accordion title="Browser support">

Polyphone uses standard WebRTC primitives. Verified browsers:

| Browser | Minimum version |
| --- | --- |
| Chrome / Chromium | 72 |
| Firefox | 60 |
| Safari (macOS) | 14.1 |
| Safari (iOS) | 16 |
| Edge | 79 |

Visitors on unsupported browsers see a disabled call button with a brief explanation. Mobile-browser and in-app-webview behaviour can vary — test in your intended deployment environment before launch.

</Accordion>

## Related pages

- [Voice](/voice/introduction) – the agent's TTS voice, shared by Polyphone and telephony
- [Numbers (telephony)](/telephony/introduction) – the phone-network route into the same voice agent
- [Webchat](/webchat/introduction) – the text channel that uses the same widget framework
- [Variant management](/variant-management) – per-domain or per-region widget configuration
- [WebRTC gateway API](/api-reference/webrtc-gateway/introduction) – the underlying transport (advanced)
