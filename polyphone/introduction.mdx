---
title: "Polyphone"
sidebarTitle: "Overview"
description: "Let visitors call your agent in one click, right from your website."
keywords:
  - polyphone
  - voice widget
  - browser voice
  - click to call
  - voice channel
  - webrtc
---

Polyphone puts a "Talk to our agent" button on your website. Visitors click it and start speaking to your agent — no phone number to dial, no app to install. Set it up in **Channels > Widgets** in Agent Studio.

It's the same agent that answers your phone numbers, with the same voice and the same conversation flow. Polyphone is just another way for customers to reach it — through the browser instead of the phone network.

## When to use Polyphone

Reach for Polyphone when you want:

- A **"Talk to our agent" call-to-action** on your website or product.
- **One-click calling** without asking visitors to find or dial a number.
- A **branded voice experience** that lives inside your site's look and feel.
- A widget that **reuses your existing agent** — no parallel build-out.

For text-only conversations on your site, use [Webchat](/webchat/introduction). For phone calls from anywhere, use [Numbers](/telephony/introduction). Polyphone, Webchat, and Numbers all share the same agent — you can run any combination.

## Polyphone in the channel mix

Polyphone is a **voice channel**, just like a phone number. The only thing that changes between them is how the audio gets to PolyAI.

| | Polyphone | Numbers (telephony) | Chat (webchat) | Voice (TTS settings) |
| --- | --- | --- | --- | --- |
| What customers do | Click a button on your site | Dial a phone number | Type in a widget on your site | – (configures how your agent sounds) |
| Modality | Voice | Voice | Text | Shared by all voice channels |
| Where you set it up | Channels > Widgets | Channels > Numbers | Channels > Widgets | Channels > Voice |
| Docs | This section | [Numbers](/telephony/introduction) | [Webchat](/webchat/introduction) | [Voice](/voice/introduction) |

Polyphone and Numbers are two ways into the same voice agent. They share the agent's spoken [voice](/voice/introduction) (set once, under **Channels > Voice**) and the same conversation logic — switching a visitor between them doesn't require a separate flow.

## What you get out of the box

- **A configurable launcher** — colour, position, and label match your site.
- **A consent step** — visitors agree to mic access and your privacy notice before the call connects.
- **Mute / end-call controls** — standard call UI built into the widget.
- **Cross-tab safety** — only one call can be active per visitor, even if they have multiple tabs open.
- **Domain allow-list** — the widget only loads on domains you've authorised.

## Prerequisites

Before you create a Polyphone widget, you'll need:

- An Agent Studio project with a **published variant**.
- The **website domain(s)** where the widget will be embedded.
- Access to your website's HTML or your tag manager.
- An **HTTPS** site — browsers require a secure context for microphone access.

You don't need a chat agent to use Polyphone — you can ship a voice-only widget. (Behind the scenes, Polyphone and Webchat share a widget framework and a single Iris script endpoint, which is why both live under **Channels > Widgets** in Studio.)

## Channel pages

<CardGroup cols={2}>
  <Card title="Widget" href="/polyphone/widget" icon="phone">
    Create, style, and configure a Polyphone widget in Agent Studio.
  </Card>
  <Card title="Installation" href="/polyphone/widget-installation" icon="code">
    Embed the script tag on your website with HTML or Tag Manager.
  </Card>
  <Card title="Test widget" href="/polyphone/test-widget" icon="circle-play">
    Walk through a real call from the editor preview link.
  </Card>
  <Card title="Troubleshooting" href="/polyphone/widget-troubleshooting" icon="wrench">
    Mic permissions, ICE failures, cross-tab lock, and more.
  </Card>
</CardGroup>

## Technical details

<Accordion title="How a call connects">

```
Visitor's browser                           PolyAI
─────────────────────                       ──────────────
Polyphone iframe (mic, autoplay)
   │
   ├── HTTPS  ──────► Iris script endpoint  (loads widget JS)
   │
   └── WSS    ──────► WebRTC gateway        (signaling + Opus audio)
                            │
                            └─► your agent
```

Polyphone is loaded by a single `<script>` tag on your site. The script injects an iframe pinned to the bottom-right of the page. When a visitor clicks the launcher, the widget requests microphone access, opens a WebSocket to PolyAI's WebRTC gateway, and starts an Opus 48 kHz audio call with your agent.

Only **one Polyphone call can be active per browser** at a time. The widget enforces this with a cross-tab lock so a visitor with multiple tabs open cannot start parallel calls.

</Accordion>

<Accordion title="Polyphone vs. telephony, side by side">

| | Polyphone | Telephony |
| --- | --- | --- |
| Transport | WebRTC over the browser | Phone network (PSTN / SIP) |
| Visitor sees | A widget on your website | A phone number to dial |
| Latency / quality | Opus 48 kHz over WebRTC | Carrier-dependent |
| Setup | Embed a script tag | Provision a number, configure SIP |
| Best for | Web-first journeys, support deflection | Inbound call centres, omnichannel routing |

You can run both: Polyphone for visitors already on your site, telephony for callers from anywhere else. Both connect to the same agent.

</Accordion>

<Accordion title="Programmatic control (JavaScript SDK)">

The widget script also exposes a `window.PolyphoneAPI` object that lets your page drive the call programmatically — useful when you want to trigger Polyphone from a custom button, an in-app event, or an existing chat experience.

```js
// Open the widget and start a call in one step.
window.PolyphoneAPI.connectAndCall();

// Pass context to the agent mid-call.
window.PolyphoneAPI.sendInfo("user_signed_in", { userId: "abc123" });

// React to lifecycle events.
window.PolyphoneAPI.on("connected", () => console.log("Call connected"));
window.PolyphoneAPI.on("ended", () => console.log("Call ended"));
```

Available commands: `connect`, `disconnect`, `startCall`, `endCall`, `connectAndCall`, `mute`, `unmute`, `sendInfo`, `isConnected`, `getCurrentSession`. Events include `ready`, `connecting`, `connected`, `ringing`, `answered`, `ended`, `failed`, `muted`, `unmuted`, `transcription`, `infoSent`, `infoReceived`, `error`.

</Accordion>

<Accordion title="Browser support">

Polyphone uses standard WebRTC primitives. Verified browsers:

| Browser | Minimum version |
| --- | --- |
| Chrome / Chromium | 72 |
| Firefox | 60 |
| Safari (macOS) | 14.1 |
| Safari (iOS) | 16 |
| Edge | 79 |

Visitors on unsupported browsers see a disabled call button with a brief explanation.

</Accordion>

## Related pages

- [Voice](/voice/introduction) – the agent's TTS voice, shared by Polyphone and telephony
- [Numbers (telephony)](/telephony/introduction) – the phone-network route into the same voice agent
- [Webchat](/webchat/introduction) – the text channel that uses the same widget framework
- [Variant management](/variant-management) – per-domain or per-region widget configuration
- [WebRTC gateway API](/api-reference/webrtc-gateway/introduction) – the underlying transport (advanced)
