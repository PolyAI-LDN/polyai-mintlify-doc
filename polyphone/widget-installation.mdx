---
title: "Widget installation"
sidebarTitle: "Installation"
description: "Embed the Polyphone widget on your website with direct HTML or Tag Manager."
---

Once your widget is configured, getting Polyphone live is a single script tag. Paste it on your site, publish, and your visitors can start calling — typically in minutes.

After [creating and configuring your Polyphone widget](/polyphone/widget), pick one of the installation routes below to ship it.

## Installation methods

Pick **one** route:

- **Option A: Direct HTML embed** — recommended for most sites.
- **Option B: Tag Manager** — when your organisation manages scripts through Google Tag Manager or similar.

<Tip>
Not sure which route to take? Start with **Option A** — it's the fastest path to live.
</Tip>

## Step 1: Generate a widget tag

1. Go to **Channels > Widgets** in Agent Studio.
2. Open the **Polyphone** tab.
3. Configure [Styling](/polyphone/widget#styling-tab) and [Content](/polyphone/widget#content-tab).
4. Click **Save and publish**.
5. Open the **Embed** tab.
6. Copy the unique script tag — it looks like this:

```html
<script src="https://messaging.poly.ai/widget/YOUR_UNIQUE_CODE.js"></script>
```

{/* TODO image — alt: "Embed tab with the Polyphone script tag ready to copy" — path: /images/polyphone/embed-script-tag.png */}
The tag is bound to the widget's domain, variant, and environment. If any of those change, re-publish and re-embed.

## Option A: Direct HTML embed

1. Open the HTML template that loads on **every page** (often called your global layout, base template, or `index.html`).
2. Paste the script tag **just before the closing `</body>` tag**.
3. Publish your website changes.

```html
<!DOCTYPE html>
<html>
  <head>
    <!-- Your existing head content -->
  </head>
  <body>
    <!-- Your page content -->

    <!-- Paste the Polyphone script here, just before </body> -->
    <script src="https://messaging.poly.ai/widget/YOUR_UNIQUE_CODE.js"></script>
  </body>
</html>
```

You only need to embed the script once. Updates to styling, content, or the agent take effect on the next page load – no re-embed required.

<Note>
The script tag must be served over **HTTPS** and the host page must be in a [secure context](https://developer.mozilla.org/en-US/docs/Web/Security/Secure_Contexts). Browsers block microphone access on insecure origins, so the call button will be disabled on `http://` pages.
</Note>

## Option B: Tag Manager

1. Open your tag manager (e.g., Google Tag Manager) and create a new **Custom HTML tag**.
2. Paste the script tag.
3. Set the trigger to **All Pages** (or restrict to the pages where you want the widget).
4. Publish the container.

The widget appears on the next page load.

## Re-embedding

You only need to re-embed when **the script tag itself changes**. The Embed tab in Agent Studio shows a **Snippet changed** banner if the published script differs from the version your team has on the site. Hand the new tag to your dev team or update your tag manager configuration.

You do **not** need to re-embed when you change:

- Branding (header text, logo, agent name, primary colour)
- CTA copy or disclaimer
- Policy links
- The agent or variant the widget connects to (within the same widget configuration)

## Verifying the install

1. Load the page where you embedded the script.
2. The Polyphone launcher (a circular button with a phone icon) appears in the bottom-right corner.
3. Click the launcher — the widget expands.
4. Click **Start call** — the browser asks for microphone permission.
5. Allow the permission — the widget shows **Connecting…** and then **In call** when your agent answers.

{/* TODO image — alt: "Polyphone launcher and expanded widget on a live site" — path: /images/polyphone/verified-install.png */}
If anything looks off, [Troubleshooting](/polyphone/widget-troubleshooting) covers the handful of issues we typically see.

## Cross-tab behaviour

When a visitor opens your site in multiple tabs, Polyphone keeps things clean: **one active call per browser**. The widget writes a heartbeat to `localStorage` while a call is live, and other tabs show the call button disabled with a "Call active in another tab" message. Closing the active tab releases the lock within ~10 seconds.

It's automatic — no configuration needed.

## Next steps

<CardGroup cols={2}>
  <Card title="Test widget" href="/polyphone/test-widget" icon="circle-play">
    Walk through a real call from the editor preview link.
  </Card>
  <Card title="Troubleshooting" href="/polyphone/widget-troubleshooting" icon="wrench">
    Common installation issues and fixes.
  </Card>
</CardGroup>
