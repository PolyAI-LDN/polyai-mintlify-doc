Enriches the **Logging API responses** section on `tools/classes/conv-api.mdx`:

- Names the method explicitly.
- Lists what gets recorded (URL, method, status, response time, error body on non-2xx).
- Adds a **Grouping by URL pattern** subsection documenting the `override_url` parameter for high-cardinality path variables.

Verified against `genai_lambda_runtime/python/runtime/conversation.py:1703`.