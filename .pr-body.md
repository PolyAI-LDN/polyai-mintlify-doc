Cross-referenced Oisin's critiques against `poly_core` and `platform_ui` and corrected wrong info in three pages.

## `api-reference/outbound/endpoint/trigger-call.mdx`
Verified against `poly_core` (`src/agent_v3/smart_qa/api_client.py`, `src/utils/e2e_testing/utils.py`, `src/agent_v3/cli/outbound_testing.py`):
- Endpoint path corrected: `POST /v1/outbound-calling` (was `/v1/outbound-calling/trigger`). This also fixes the URL shown in the API playground "Try it" panel.
- Added `metadata` body parameter (object of string-only values) with note that values are exposed to the agent via `conv.sip_headers.get('key_name')`.
- Added optional `encryption` body parameter (`UDP/RTP` default, `TLS/SRTP`).
- Response field renamed `callSid` → `call_sid` to match actual server response (confirmed in `outbound_testing.py:310`).
- Rewrote description per Oisin: removed "queued" wording, added that calls are placed close to immediately, the start tool runs **after** the call connects, and a compliance/lawful-use warning.
- Updated example payload to match the realistic body Oisin shared.

Also fixed `get-call-status.mdx` so the path param and response field use `call_sid` consistently.

## `learn/guides/get-started/create-a-project.mdx`
Verified against `platform_ui` (`ProjectIdInput`, `createAssistant.uiCopy.ts`, `ImportAssistantModal`):
- Added an explicit warning that **once set, the Project ID cannot be changed** and that an environment suffix is appended automatically.
- Softened "impossible to tell which agent" → "hard to tell" per Oisin's pushback.
- Reworded the generic-name explanation so it no longer conflates project naming with environments and variants.

## `learn/guides/get-started/edit-agent-behavior.mdx`
Verified against `platform_ui` (`ProjectNavigation.uiCopy.ts`, `PersonalityInput/types.ts`, `ChannelSettings/Greeting`) and `poly_core` (`src/poly_platform/llemur/conditional_content.py` + tests):
- Removed the duplicate "Boundaries" card – it was just a subset of Behavior. The Build → Agent screen has three sections (Personality, Role, Behavior); cards now reflect that.
- Removed Greeting from the Build → Agent description – greeting is configured per-channel under Voice / Channel settings, not on the Agent page.
- "Functions page" → "Tools page" (navigation label was renamed in `ProjectNavigation.uiCopy.ts`: `functions: 'Tools'`).
- Renamed the misleading "Agent section" heading to "Role", and reworded the scope/handoff example to point at a tool + Behavior rule instead of an undefined "transfer to billing" instruction.
- Listed the actual built-in personality options (`Polite`, `Kind`, `Funny`, `Energetic`, `Calm`, `Thoughtful`, `Other`) rather than fabricated ones, and clarified that the freeform example is what you write when "Other" is selected.
- Added a tip clarifying that most detailed prompting lives in the Behavior section (matches actual customer agents like Brittain/Golden Entertainment).
- Fixed `<channel:voice>` and `<language:*>` examples to use **closing tags** (`</channel>`, `</language>`) – required per `conditional_content.py`. Also documented nesting.
- Reworded the "Test edge cases" step so it no longer implies guardrails are 100% built-in – they're enforced by the prompt the builder writes.
- Updated the "saved a new greeting" quiz question so it references channel settings rather than the (no-longer-existent) greeting field on the Agent page.

## Open product questions (not changed in this PR)
A few items in the critique are product/UX decisions rather than doc errors – flagging here so they can be picked up separately:
- The "Agent / Behavior toggle" UX overlap with the "Writing effective behavior configuration" heading.
- Whether the create-project page should document what each default personality value actually does.
- Mermaid diagram overlap with the image navigation control on `edit-agent-behavior` – likely a Mintlify rendering issue worth looking at.