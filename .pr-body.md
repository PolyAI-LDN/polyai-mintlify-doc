Stacks on top of #368 (rename No code mode → Free trial mode).

## What changed

Free trial mode previously **hid** enterprise/developer sidebar entries entirely and auto-exited the mode if a user clicked a link to an enterprise page. Per Aaron's spec ("greyed out and bannered/uninteractive rather than missing"), it now:

1. **Greys out** enterprise sidebar entries instead of hiding them — opacity 0.45, `pointer-events: none`, `cursor: not-allowed`.
2. **Badges** them with an inline `Enterprise` pill (CSS pseudo-element, no DOM mutation per item).
3. **Renders an upsell banner** at the top of any enterprise/developer page when reached in free trial mode (search, deep link, manual URL). The banner has a "Talk to sales" CTA and an "Exit free trial mode" button that drops you into full docs in place.
4. **No more auto-exit on click** — clicking a greyed sidebar item is a no-op. The mode persists.

Top-nav tabs (Developer, API reference, Advanced) stay hidden — a greyed-out tab looks broken.

## Why one PR for B + C

Per the previous discussion this would have been two PRs (B = behaviour, C = page tagging). The existing path-prefix and tag-detection logic in `simplify-toggle.js` already covers the full enterprise-only page list, so no per-page frontmatter pass was needed. PR-C collapses to zero MDX edits.

## Files

- `scripts/simplify-toggle.js` — replace `data-simplifiedHide` with `data-simplifiedEnterprise` for sidebar items; new `applyEnterpriseBanner()` function inserts/removes the banner on nav and on toggle; click-handler swallows clicks on greyed entries; `pushState` preserves `?view=simplified` even on enterprise destinations.
- `styles.css` — new `[data-simplified-enterprise="true"]` rules (greyed + badge), `.free-trial-enterprise-banner` and child class styles. The `[data-simplified-hide]` rule remains for nav tabs.
- `platform/free-trial-mode.mdx` — copy update: cards/exit instructions describe the new grey-out + banner behaviour.

## Validation

- `mint validate` ✅
- `mint broken-links` ✅ (one pre-existing unrelated break in `learn/recipes/introduction.mdx`)

## Manual smoke test (after merge / preview build)

1. Toggle ON → confirm `Developer tools`, `Secrets`, `Tools`, `Variant management`, `Test suite`, `APIs`, `Configuration builder` etc. are greyed and badged "Enterprise" in the sidebar.
2. Click a greyed item → nothing happens.
3. Type `/api-reference/introduction` into the URL bar → page loads with the upsell banner at the top, sidebar still in free trial state.
4. Click "Exit free trial mode" in the banner → page re-renders without the banner, sidebar restored, pill flips to off.
5. Mixed pages (`/call-handoff/introduction`, `/sms/introduction`, `/flows/introduction`) stay accessible without a banner; their `.developer-only` blocks are still wrapped in the existing accordion.