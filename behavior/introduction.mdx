---
title: "Behavior"
sidebarTitle: "Overview"
description: "Set your agent's role, behavior, language, guardrails and model."
mode: wide
keywords:
  - agent settings
  - configuration
  - role
  - behavior
  - system prompt
  - rules
  - agent config
---

The **Behavior** page has four tabs: **General** (role and behavior), **Language**, **Guardrails**, and **Models**.

![Behavior > General overview](/images/agent-settings/agent-overview.png)

## When to configure each setting

| Setting | When to use | Impact |
|---------|------------|--------|
| **[General](/behavior/general/agent)** | Initial setup, rebranding, or any change to how the agent speaks and behaves | The **Role** and **Behavior** fields, which together form the system prompt. The greeting is configured per channel under **Voice/Chat configuration**. |
| **[Prompting guide](/behavior/general/rules)** | Before writing or editing the Behavior field | Structure, a full example, and what belongs in Knowledge, Flows, Tools or Guardrails instead. |
| **[Model](/behavior/models/model-use)** | New agent setup, or optimizing for cost/latency/quality | LLM selection, set per channel. [Raven](/behavior/models/raven) recommended, but other models are also available. |
| **[Guardrails](/behavior/guardrails/introduction)** | Safety protections against jailbreaks, hallucination, crisis escalation, and your own hard rules | Platform and custom guardrails, enforced outside the prompt. Lives under **Behavior > Guardrails**. |

## Configuration sections

<CardGroup cols={2}>
  <Card title="General" icon="user" href="/behavior/general/agent">
    Role and behavior: the agent's system prompt.
  </Card>
  <Card title="Model" icon="brain" href="/behavior/models/model-use">
    Raven (recommended), OpenAI, Amazon Bedrock, or your own endpoint.
  </Card>
  <Card title="Prompting guide" icon="pen" href="/behavior/general/rules">
    How to write Role and Behavior, and what to keep out of them.
  </Card>
  <Card title="Guardrails" icon="shield" href="/behavior/guardrails/introduction">
    Platform protections against jailbreaks, hallucination and crisis events, plus your own custom guardrails.
  </Card>
  <Card title="Language" icon="language" href="/behavior/language/multilingual">
    Multilingual settings, language coverage, and translation overrides.
  </Card>
</CardGroup>

## Automate with the Agents API

If you spin up new agents from a reference template — for example, one agent per client or market — you can create and duplicate them from code.

<AccordionGroup>
  <Accordion title="Script agent creation via the Agents API" icon="code">
    The [Agents API](/api-reference/agents/introduction) has endpoints for [creating](/api-reference/agents/endpoint/agents/create-agent) and [duplicating](/api-reference/agents/endpoint/agents/duplicate-agent) agents.

    ```bash
    # Create a new agent from scratch
    curl -X POST https://api.us.poly.ai/v1/agents \
      -H "x-api-key: $POLYAI_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{ "name": "Support Agent", "description": "Tier 1 support, US" }'

    # Duplicate an existing agent as a starting point
    curl -X POST https://api.us.poly.ai/v1/agents/AGENT_ID/duplicate \
      -H "x-api-key: $POLYAI_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{ "name": "Support Agent - UK" }'
    ```
  </Accordion>
</AccordionGroup>

## Related pages

<CardGroup cols={2}>
  <Card title="Agents endpoints" icon="square-terminal" href="/api-reference/agents/endpoint/agents/create-agent">
    Create, duplicate, and configure agents from code.
  </Card>
  <Card title="Deployments" icon="server" href="/environments-and-versions/introduction">
    Build and test in Sandbox, check in Staging if you need to, then publish to Live.
  </Card>
</CardGroup>

