---
title: "Update a SIP Trunk"
openapi: "PATCH /v1/accounts/{account_id}/telephony/sip-trunks/{trunk_id}"
---

## Clearing inbound authentication

To clear an inbound authentication scheme, send `{ "disable": true }` in place of its credentials — for example, `"inbound": { "sip_token_auth": { "disable": true } }`.
