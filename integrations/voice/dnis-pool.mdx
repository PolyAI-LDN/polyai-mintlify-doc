---
title: 'DNIs Pool'
description: 'Integrate your telephony system with PolyAI using a pool of Dynamic Number Insertion (DNI) numbers.'
---

A DNIs Pool is a rotating pool of Dynamic Number Insertion (DNI) numbers that lets you connect to your Virtual Agent hosted on PolyAI's infrastructure. This integration uses a combination of a PolyAI API and PSTN numbers to pass metadata when SIP headers are unavailable or not useful for information sharing.

## Overview

DNI pooling lets you:
- Share structured JSON metadata with your agent at the start of a call
- Retrieve call state information at the end of the conversation
- Avoid custom SIP header requirements

This is useful when:
- The system you are integrating to PolyAI is capable of calling the `/reserve_dni` and '/handoff_state` APIs
- You're using PSTN, which does not support sharing metadata
- You're using SIP installations that do not support custom SIP headers
- You're using SIP and the unique ids that are passed to PolyAI are not availbale to the system that can call the Handoff API.

A typical use case is sending a shared ID (or other metadata) at call start, and using it later to retrieve handoff details, such as whether the call completed successfully or needs escalation to a human agent.

## Requirements

To set up the DNIs Pool integration, you'll need the following from your PolyAI contact:

- API token for the `Reserve DNI` endpoint
- API token for the `Get Handoff` endpoint
- Your `account_id` and `project_id`
- Agree with your PolyAI contact on the attributes the Agent will need to extract
- PolyAI will setup a pool of numbers for your project.


## Integration Runtime

The integration involves two main API endpoints:
- [`Reserve DNI`](/api-reference/dni/endpoint/dni-reservation)
- [`Get Handoff`](/api-reference/handoff/endpoint/get-handoff)

![DNIs Pooling Integration](/images/integrations/dnis-pooling-integration.jpg)

### Call flow

The integration flow works as follows:

1. Client sends project-specific attributes to the Reserve DNI endpoint. An example payload would look like this passing a shared id and in this case the calling number of the caller and the dnis/originally dialled number:
```json
{
    "attributes": {
        "shared_id": "1234567",
        "ani": "+44123456789",
        "dnis": "+44987654321"
    }
}
```
2. PolyAI stores the attributes and returns a reserved DNI (e.g., `+12345`).
3. Client initiates a call to the DNI.
4. Attributes are available to the Virtual Agent in the start tool. Note once the call arrives at PolyAI the number in the pool is marked free for re-use, it is not reserved for the duration of the conversation.
5. The Virtual Agent handles the conversation and stores any agreed data (e.g., handoff status).
6. The call ends, and PolyAI sends a SIP BYE message.
7. Client queries the Get Handoff endpoint to retrieve the call outcome. 
An example response for the handoff API will look like this - note that the `shared_id` is the same ID you would have sent above:
```json
{
    "data": {
        "handoff_to": "default_queue",
        "handoff_reason": "test_reason"
    },
    "id": "POLYAI_CALL_SID",
    "shared_id": "1234567"
}
```
9. If a handoff state is present, the Client can transfer the call to a human agent; otherwise, the call is considered complete.
<Tip>If the system that has passed the call to PolyAI is capable of PolyAI ending the call and then the system calls the Handoff API to determine the next action this is the preferred/more efficient pattern since this removes unneeded PSTN legs.</Tip>

## Agent setup

For the integration to work, your agent needs to:
- Store the attributes you are sending through the [`Reserve DNI`](/api-reference/dni/endpoint/dni-reservation) API
- Save the handoff info into state to have the [`Get Handoff`](/api-reference/handoff/endpoint/get-handoff) API expose them when invoked

### Store attributes
Attributes received from the API can **only** be accessed in the `start_function`.
Not doing it here would mean losing them for all the conversations, making it impossible for the integration to work.

In your `start_function`, attributes are available in the `integration_attributes` field as a dictionary.
Retrieve them like this (assuming the attributes sent are the ones in the example above):
```python
#### anywhere in start_function ####

# Retrieve DNIs Pooling Attributes into local variables in the start_function
shared_id = conv.integration_attributes.get("shared_id")
ani = conv.integration_attributes.get("ani")
dnis = conv.integration_attributes.get("dnis")
if not (shared_id or ani):
  # In case no attributes are received, make sure to handoff to a default queue 
  # on your side if you can't handle the call without the data
  conv.state.handoff_to = "DEFAULT_QUEUE"

print(f"shared_id: {shared_id}, ani: {ani}")  # you can optionally print the attributes

# Store them in the state to use them later in the conversation, they could be directly stored to conv.state above.
conv.state.shared_id = shared_id
conv.state.ani = ani
conv.state.ani = dnis
```
You can then use those attributes in other functions, in the FAQs or elsewhere
in your Agent. This will be useful to customize your Agent behavior, or use those attributes
in other API calls to your infrastructure for example.

### Save handoff info

The conversation will always end with this integration. Handoff information must be prepared to be exposed by the Handoff API.

The Handoff API can be configured to expose any of the `state` variables you desire. The most useful variables are often:
- `conv.state.handoff_reason` - the reason for handoff
- `conv.state.handoff_to` - the destination for the call

As long as you store your desired values in `state` variables, the Handoff API will expose them (if configured to do so).

Configuration is handled by your PolyAI contact. Send them all the variables you want to expose, and they will set up the Handoff API accordingly.

## Pool size 
It's important to make sure that every customer hitting your solution gets to PolyAI.
The DNIs pool **does not** need to be sized for the total concurrent call volume. 
A number is only reserved for a specific call from the time the `/dni-reservation` API is called to when the call arrives at PolyAI and the attributes are made available to the Virtual Agent. This generally only takes a second or two at the most. 

As an example if we take a conservative approach of 4 seconds per call of the number being reserved and assume a pool of 20 numbers is available:

Per line: 60s ÷ 4s = 15 calls/minute
Max calls/minute (20 lines): 20 × 15 = 300 calls/minute
In 15 minutes: 300 × 15 = 4,500 calls
In 1 hour: 300 × 60 = 18,000 calls

<Note>The actual duration of these calls is not a factor they can last for many minutes and it does not affect the capacity of the DNI Pool</Note>

The pool size is configured by your PolyAI contact.

## Fallback handling

If no DNI is available for your call, the Reserve DNI API returns a 404 status code. Set up your telephony system to handle this case by routing calls to a fallback queue.

PolyAI monitors pool utilization per project and will be notified automatically, enabling prompt action to extend the pool size.