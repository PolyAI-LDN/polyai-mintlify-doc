---
title: 'Environments'
sidebarTitle: 'Environments'
description: 'What Sandbox, Staging and Live are for, and what each one connects to.'
keywords:
  - environments
  - sandbox
  - staging
  - live
  - deployments
---

Every project has three environments. You build in Sandbox, optionally test on your channels in Staging, and serve customers from Live.

## Sandbox

Sandbox is where you build. Every Branch and Sub-branch lives here, as does your working copy in [Simple mode](/environments-and-versions/simple-mode).

You test in Sandbox by running [simulation tests](/testing/simulation-tests), or by talking to your agent over an in-app call or chat. Your API and app connections work here, using the credentials set up for Sandbox, so your integrations behave as configured. Every Branch shares those connections.

Sandbox has no channel connections. A channel connection points at one version of your agent, and Sandbox holds every Branch at once. There would be no way to tell which should answer. Staging and Live each run a single version, so that is where channel connections live.

## Staging

Staging is optional, and it holds one Branch at a time. That is what lets it have its own channel connections, so you can reach a Branch the way a customer would.

It also has its own integration credentials, so most teams point Staging at test accounts and services rather than production ones. See [Staging](/environments-and-versions/staging) for how to set it up and move a Branch into it.

## Live

<Frame caption="Live history on the Deployments page, with every published version in order">
  <img src="/images/deployment/deployments-live-history.png" alt="Deployments page Live history tab, listing published versions with the current one marked Live" style={{ maxWidth: '720px', width: '100%', margin: '0 auto', display: 'block' }} />
</Frame>

Live is the version serving your customers. You never edit it directly.

Publishing merges your changes onto Live and creates a new version in its history. If something goes wrong, you can roll Live back to any previous version in one step.

## What each environment gives you

| | **Sandbox** | **Staging** | **Live** |
|---|---|---|---|
| **What it is for** | Building and testing in the app | Testing on a channel | Serving customers |
| **Branches** | Every Branch and Sub-branch | One at a time | The published version |
| **Channel connections** | None | Its own | Your production ones |
| **Integrations** | Shared by every Branch | Its own | Your production credentials |
| **Who reaches it** | Agent Studio users with access to your project | Anyone you give the details to | Your customers |

## What is set per environment

- **Channel connections.** A channel connection points at one environment. Staging and Live each have their own, and Sandbox has none. See [Staging](/environments-and-versions/staging) for how to set them up.
- **Integrations.** Apps, the [APIs](/integrations/api/introduction) you add yourself, and [knowledge sources](/knowledge/sources/introduction) each hold their own configuration per environment. Branches in Sandbox share one set between them.
- **Real-time configuration.** Values and schemas are set per environment, and changes take effect immediately rather than waiting for a publish. See [Real-time configuration](/real-time-config/introduction).

Functions can also behave differently per environment by reading `conv.env`. A custom handoff function can use it to transfer to a different destination in Staging than on Live. See [Environment configuration](/tools/variables#environment-configuration).

<Note>
MCP servers are the exception. They are not configured per environment, so an agent connects to the same MCP server wherever it runs.
</Note>

## Related pages

<CardGroup cols={2}>
  <Card title="Deployments" icon="rocket" href="/environments-and-versions/introduction">
    How Live, Branches, Sub-branches and the deployment modes fit together.
  </Card>
  <Card title="Staging" icon="flask" href="/environments-and-versions/staging">
    Set up Staging and move a Branch into it.
  </Card>
  <Card title="Compare changes" icon="code-compare" href="/environments-and-versions/diffs">
    See what changed before you publish.
  </Card>
  <Card title="Environments and testing FAQ" icon="circle-question" href="/troubleshoot/faq-environments">
    Common questions about Sandbox, Staging and Live.
  </Card>
</CardGroup>
