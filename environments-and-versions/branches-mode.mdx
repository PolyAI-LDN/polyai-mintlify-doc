---
title: 'Branches mode'
sidebarTitle: 'Branches mode'
description: 'Work in your own Branch, test it safely, and publish it onto Live.'
keywords:
  - deployments
  - branches
  - publish
  - staging
  - sandbox
---

Branches mode is for small teams who want isolated changes and review before they go live. Each person works in their own Branch and publishes it onto Live on their own schedule.

<img className="block dark:hidden" src="/images/deployment/deployment-branches-mode-light.svg" alt="Branches mode: Branches in Sandbox publish onto Live, with an optional move to Staging" style={{ maxWidth: '920px', width: '100%', margin: '0 auto' }} />
<img className="hidden dark:block" src="/images/deployment/deployment-branches-mode-dark.svg" alt="Branches mode: Branches in Sandbox publish onto Live, with an optional move to Staging" style={{ maxWidth: '920px', width: '100%', margin: '0 auto' }} />

## How it works

A Branch is a safe place to work: an isolated copy of your agent, branched off Live, living in the Sandbox environment with no API or telephony connections. Your Branch saves as you go, and you can test it at any time by running [simulation tests](/testing/simulation-tests) or by talking to your agent over an in-app call or chat. If you want to test with a real phone number and API connections, move it to Staging. When ready, publish: your Branch merges onto Live and becomes the newest version in Live's history.

<Tip>
**Publishing merges, it does not replace.** If Live has moved on since you created your Branch, perhaps because a colleague published first, your Branch is marked **Out of sync**. Syncing pulls the latest changes from Live into your Branch. If you have both touched the same thing, a conflict screen lets you decide which edit to keep. Nothing is silently lost.
</Tip>

Nobody queues behind anyone. While one colleague spends weeks building a new flow in their Branch, another can open a Branch of their own, update the knowledge base, and publish the same day without affecting the first colleague's work.

<Warning>
Branches cannot merge into each other. Each one publishes to Live on its own. To split one piece of work across several people, use [Sub-branches mode](/environments-and-versions/sub-branches-mode).
</Warning>

## Make a small change

The everyday case: a knowledge update, a settings change, a behaviour prompt tweak.

<Steps>
  <Step title="Create a Branch from Live">
    It branches off the current Live version, so you start from what is actually running.
  </Step>
  <Step title="Make your change">
    It saves as you go.
  </Step>
  <Step title="Test it">
    Run [simulation tests](/testing/simulation-tests) on the Branch, or talk to the agent yourself over an in-app call or chat.
  </Step>
  <Step title="Move to Staging (optional)">
    Move the Branch to Staging from its actions menu on the Deployments page, then call the number.

    <Tip>
    Skip this step unless you want to test with a real phone number and API connections.
    </Tip>
  </Step>
  <Step title="Publish">
    The changes in your Branch merge onto Live and become the newest version in Live's history.
  </Step>
</Steps>

Nothing else rides along with your change, and nobody else's work is touched.

## Handle an out of sync Branch

This happens whenever a colleague publishes onto Live before you do.

<Steps>
  <Step title="Your Branch is marked Out of sync">
    This is expected, not an error.
  </Step>
  <Step title="Sync">
    This pulls the latest changes from Live into your Branch.
  </Step>
  <Step title="Resolve any conflicts">
    If you have both edited the same thing, a conflict resolution screen asks which edit to keep.
  </Step>
  <Step title="Test, then publish">
    Check the merged result before you publish.
  </Step>
</Steps>
